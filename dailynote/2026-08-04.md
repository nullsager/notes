```python
class Embedding(nn.Module):
    def __init__(self, vocab_size: int, hidden_size: int):
        super().__init__()
        # 创建一个可学习的查找表
        self.weight = nn.Parameter(torch.randn(vocab_size, hidden_size))

    def forward(self, token_ids: torch.Tensor) -> torch.Tensor:
        # token_ids: (batch_size, seq_len)
        # 返回: (batch_size, seq_len, hidden_size)
        return self.weight[token_ids]
```
1.  `nn.Module`什么情况要继承？继承了有什么用
2. `super().__init__()`是什么？`super`什么情况用？和self有什么区别？
3. `nn.Parameter`怎么用？只有继承了`nn.Module`才能用吗
4. `torch.randn`函数怎么用？
5. forward 函数是什么意思？weight 的结构是什么？为什么`self.weight[token_ids]`能返回`(batch_size, seq_len, hidden_size)`


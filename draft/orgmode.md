# Org mode 使用手册

> 本文是 nvim-orgmode 的日常使用教程，面向"任务管理 + 时间规划"的完整工作流。
> 配置说明见 [README.md](README.md) 的 5.10 / 6.10 节；快捷键总表也在那里。
>
> **两条救命稻草**：在 org 文件里忘了按键就按 `g?`（插件内置按键帮助）；
> 想看官方完整文档就执行 `:Org help`。

---

## 一、30 秒理解 Org mode

Org mode 的本质是**纯文本大纲 + 任务属性**：

- 一行星号开头的就是一个标题，星号个数 = 层级（`*` 一级、`**` 二级……）
- 标题可以附加：**TODO 状态**、**标签**、**优先级**、**日期**（计划/死线/重复）
- 散在各文件里的任务，由 **Agenda（议程）** 按日期自动汇总成视图
- 灵光一闪用 **Capture（速记）** 10 秒记下，事后再用 **Refile（归档移动）** 整理

**本配置的目录约定**：

| 项目 | 位置 |
|------|------|
| Org 文件目录 | `~/Documents/org`（启动时自动创建） |
| 速记默认落点 | `~/Documents/org/refile.org`（GTD 的"收集箱"） |
| Agenda 扫描范围 | 该目录下所有 `.org` 文件 |
| 与 Obsidian 分工 | Markdown 笔记（知识沉淀）→ Obsidian；任务日程 → Org mode |

---

## 二、基础语法速查

一个典型的 org 文件长这样：

```org
* TODO 写季度总结报告 :工作:汇报:
SCHEDULED: <2026-08-15 Sat> DEADLINE: <2026-08-20 Thu -3d>
** DONE 收集销售数据
** TODO 撰写初稿 [#A]
   - [ ] 整理 Q2 数据
   - [ ] 整理 Q3 预测
** 参考资料
   [[https://example.com][去年同期的报告]]
```

逐个拆解：

### 2.1 标题与结构

- `*` 一级标题、`**` 二级标题，以此类推，层级不限
- `<Tab>` 在标题上折叠/展开当前子树，`<S-Tab>` 循环全文折叠级别
- 打开文件默认只显示顶层标题（`overview` 模式），一眼看清全局
- `<<` / `>>` 调整当前标题层级；`<s` / `>s` 调整整棵子树
- `<leader>oK` / `<leader>oJ` 把子树整体上下移动

### 2.2 TODO 状态

- 标题前加 `TODO` 即成为任务：`TODO → DONE` 循环（`cit` 前进、`ciT` 后退）
- 切到 `DONE` 时自动在标题下记录完成时间（`CLOSED: [...]`）
- 默认只有 `TODO | DONE` 两个状态，可自定义（见"五、进阶"）

### 2.3 标签与优先级

- 标签写在标题末尾：`:工作:汇报:`（冒号包围，可多个），用 `<leader>ot` 修改
- 优先级写在 TODO 后：`[#A]`（最高）/ `[#B]`（默认）/ `[#C]`，用 `<leader>o,` 设置，`ciR` / `cir` 升降
- Agenda 里按优先级排序，标签可用于过滤

### 2.4 日期与时间（重点）

org 的时间戳分两类：

| 类型 | 写法 | 含义 |
|------|------|------|
| 主动时间戳 | `<2026-08-13 Thu>` | 会出现在 Agenda 对应日期上 |
| 非主动时间戳 | `[2026-08-13 Thu]` | 纯记录，不进 Agenda（如完成时间） |

任务的两个关键日期：

| 关键字 | 含义 | 设置快捷键 |
|--------|------|------------|
| `SCHEDULED:` | 计划**何时开始做** | `<leader>ois` |
| `DEADLINE:` | **何时必须完成**（死线） | `<leader>oid` |

- DEADLINE 默认提前 **14 天**开始在 Agenda 里预警；
  单个任务可用 `<2026-08-20 Thu -3d>` 改为提前 3 天
- 光标在日期上时：`<C-a>` / `<C-x>` 增减时间、`<S-Up>` / `<S-Down>` 按天增减、
  `cid` 弹日历修改
- 插入模式输入 `:today:` / `:now:` 后按空格，自动展开为当前日期 / 时间戳
  （另有 `:itoday:` / `:inow:` 展开为非主动时间戳）

**重复任务**（repeater，加在时间戳末尾）：

| 写法 | 完成后的新日期 | 适合 |
|------|----------------|------|
| `+1w` | 原计划日期 +1 周（若已过期会立刻再次出现在议程） | 不容错过的任务 |
| `++1w` | 不断 +1 周直到落入未来 | 例行事务（周会、账单） |
| `.+1w` | 从**实际完成当天**起算 +1 周 | "距上次一周"型（浇花、换床单） |

示例：每周五写周报 → `SCHEDULED: <2026-08-14 Fri ++1w>`

### 2.5 其他常用元素

- **复选框**：手动输入 `- [ ] ` 创建第一个；在复选框条目上按 `<leader><CR>`
  自动追加下一个；按 `<leader>mb` 在 `- [ ]` / `- [X]` 间切换
  （插件默认是 `<C-Space>`，与输入法切换键冲突，本配置已改为 `<leader>mb`，
  与 Markdown 笔记的复选框键一致）
- **链接**：`[[https://example.com][描述]]`，`<leader>oli` 插入、
  `<leader>oo` 打开光标处链接、`<leader>ols` 存储当前位置供别处引用
- **代码块**：

  ```org
  #+begin_src python
  print("hello")
  #+end_src
  ```

  插入方式（任选其一）：
  - 片段：插入模式输入 `src`，补全菜单出现后按 `<CR>` 展开骨架
    （语言名是第一个占位符，输入语言后 `<Tab>` 跳到正文位置）
  - 手敲 `#+begin_src 语言` … `#+end_src` 两行，最简单可靠
  - 另有 friendly-snippets 提供的 `<s` 代码块、`<e` 示例块、`<q` 引用块等

  编辑：把光标放在 `#+begin_src` 与 `#+end_src` **之间**，按 `<leader>o'`
  在独立窗口用完整 filetype 环境编辑（`<leader>ow` 仅保存、`<leader>ok` 放弃、
  再按 `<leader>o'` 保存返回）。注意它**只对代码块生效**——光标不在代码块内、
  或对表格等其他元素使用时，会提示 "No block node found near cursor"。
- **表格**：插入模式输入 `<t` 后按 `<CR>`（自定义片段）展开表头骨架，
  或直接手敲第一行 `| 列1 | 列2 |`；光标在表格内按 `<leader><CR>` 新增行，
  用 `gq` 重新对齐列宽
- **智能新建**：`<leader><CR>` 依上下文创建——标题上→同级标题、
  列表上→列表项、表格上→表格行

---

## 三、日常使用流程（一天的工作流）

```
早上  ──  <leader>oa → a   看本周/今日议程，规划今天
        ──  在议程里 vd 切日视图，逐项处理

随时  ──  <leader>oc → t   脑子冒出任何事，10 秒记下（进 refile.org）
        ──  <C-c> 保存继续干活，不打断心流

处理时 ──  打开对应 org 文件：cit 推进状态、<leader>ois/oid 排期
        ──  干完就 cit 变 DONE（自动记完成时间）

晚上/周末 ── 打开 refile.org，<leader>or 把速记归档到各项目文件
          ── 已完成的子树 <leader>o$ 归档，保持文件清爽
```

**速记 → 整理 → 归档**这条流水线是 Org mode 的核心心法：
收集箱（refile.org）负责"快"，定期整理负责"不乱"。

---

## 四、使用场景

### 场景 1：突然想到一件事，10 秒记下来（GTD 收集）

写代码时想起"晚上要取快递"：

1. `<leader>oc` 打开 Capture 菜单
2. 按 `t` 选 Task 模板
3. 输入 `取快递`，`<C-c>` 保存——自动追加到 `refile.org`，继续刚才的工作
4. （想放弃按 `<leader>ok`；想直接存到别的位置按 `<leader>or`）

> 心法：Capture 的意义是**不切换上下文**。不要当场整理，晚上统一处理。

### 场景 2：每天早上规划与回顾（Agenda）

1. `<leader>oa` 打开 Agenda 菜单，按 `a` 看本周议程
2. 视图操作：`vd` 日视图聚焦今天，`vw` 回周视图，`vm` / `vy` 看更长期
3. `f` / `b` 翻看下 / 上一时间段，`.` 回到今天，`J` 跳到任意日期
4. 直接在议程里处理任务（不必打开文件）：
   - `t` 切换 TODO 状态
   - `<leader>oid` / `<leader>ois` 改死线 / 计划日期
   - `<leader>ot` 改标签，`/` 按标签或关键字过滤
5. 找到要干的活：`<CR>` 跳过去开工，`q` 退出议程

菜单里其他选项：`t` 全部待办清单、`m` 按标签/属性查询、`M` 只查待办、
`s` 全文搜索（标题和正文）。

### 场景 3：管理一个多步骤项目

建一个 `~/Documents/org/网站重构.org`：

```org
* 网站重构 :项目:
** DONE 需求梳理
** TODO 设计稿评审 :设计:
SCHEDULED: <2026-08-14 Fri>
** TODO 前端开发 [#A]
*** TODO 首页改版
    - [ ] 导航栏
    - [ ] 首屏
*** TODO 接口联调
** 参考资料
   [[file:~/Documents/notes/设计规范.md][设计规范笔记]]
```

配套操作：

- 复选框 + 子标题混用，进度一目了然
- `<leader>ot` 给任务打 `:设计:` 等标签，Agenda 里按 `m` 汇总同类任务
- 用 `[[file:...]]` 链接把 Obsidian 笔记关联进来，`<leader>oo` 直接打开
- 文件多时按项目分文件，Agenda 自动跨文件汇总，无需手工维护索引

### 场景 4：周期性任务（例会、账单、习惯）

```org
* TODO 周五写周报 :工作:
SCHEDULED: <2026-08-14 Fri ++1w>
* TODO 还信用卡 :财务:
DEADLINE: <2026-08-25 Tue +1m>
* TODO 给绿萝浇水
SCHEDULED: <2026-08-13 Thu .+1w>
```

- 周报：`++1w`，完成后自动排到下个周五
- 信用卡：`+1m` + 死线预警，永远不会忘
- 浇水：`.+1w`，从实际浇水那天重新算一周

完成后 `cit` 一次即可，日期自动滚动——重复任务只需设置一次。

### 场景 5：死线管理与提前预警

```org
* TODO 提交报销单 :财务:
DEADLINE: <2026-08-20 Thu -3d>
```

- `-3d` 表示提前 3 天开始出现在 Agenda 的预警区（全局默认 14 天）
- Agenda 里 DEADLINE 任务会显示 `In 3 d.` 或 `Deadline` 字样
- 快到期时每天打开 Agenda 都能看到它逼近

### 场景 6：统计工时（Clocking）

写方案想计时：

1. 光标在任务标题上，`<leader>oxi` 开始计时（状态栏可见）
2. 干完 `<leader>oxo` 结束，时长自动记入标题下的 `:LOGBOOK:`
3. 中途切任务：直接在新任务上 `<leader>oxi`（旧的自动停）
4. `<leader>oxj` 跳回正在计时的任务
5. Agenda 里按 `R` 切换工时报表模式，看每个项目花了多少时间

### 场景 7：开会记笔记

1. 会前：`<leader>oc` → `t`，标题写会议名
2. 会中：标题下直接记要点；插入模式输入 `:now:` + 空格插入精确时间戳
3. 会上分派的任务：在要点上 `<leader>o*` 把正文行升级成标题，
   补 `TODO` 和 `<leader>oid` 死线——笔记原地变任务
4. 会后：`<leader>or` 把这棵子树 refile 到对应项目文件

### 场景 8：技术笔记与代码片段

org 文件同样适合写技术记录（与 Obsidian 的 Markdown 互补：偏"项目过程"的放这里）：

````org
* 数据库迁移记录
** 遇到的问题
   #+begin_src sql
   ALTER TABLE users ADD COLUMN avatar TEXT;
   #+end_src
** 最终方案
   ...
````

- 代码块按各自语言高亮（Treesitter）
- `<leader>o'` 在独立窗口编辑代码块，享受完整的 LSP / 补全 / 缩进环境
- `<leader>obt` 可把文件里的代码块导出（tangle）成源文件

### 场景 9：周末整理收集箱（Refile 详解）

一周后 `refile.org` 里堆了十几条速记：

1. `:e ~/Documents/org/refile.org` 打开
2. 光标放在某条任务上，`<leader>or`
3. 插件弹出目标选择：先选文件，再选要挂到哪个标题下（支持逐层下钻）
4. 回车，这条任务连同子树被移动过去
5. 全部清空后，收集箱归零——GTD 的"清空收件箱"仪式完成

### 场景 10：完成任务归档，保持文件清爽

项目文件里 DONE 的任务越积越多：

- `<leader>o$`：把当前子树移到同目录的 `xxx_archive.org`，正文里彻底消失
- `<leader>oA`：只打 `ARCHIVE` 标签——内容还在原地，但 Agenda 和折叠都会忽略它

推荐：小项目用 `<leader>oA` 就地归档，大文件定期用 `<leader>o$` 清走。

### 场景 11：导出分享

org 文件内按 `<leader>oe` 打开导出菜单，可导出 HTML / PDF / Markdown 等。

> 导出依赖系统安装 **pandoc** 或 **emacs**（`sudo pacman -S pandoc`），
> 不用导出功能可完全忽略此项。

---

## 五、进阶自定义

改 `lua/plugins/orgmode.lua` 的 `setup({...})`，以下片段可直接加进去。

### 5.1 更多 TODO 状态

```lua
org_todo_keywords = { "TODO", "NEXT", "WAITING", "|", "DONE", "CANCELED" },
```

竖线 `|` 前是"未完成"状态、后是"完成"状态；`cit` 按顺序循环。

### 5.2 更多 Capture 模板

```lua
org_capture_templates = {
  t = { description = "Task",   template = "* TODO %?\n  %u" },
  n = { description = "Note",   template = "* %?\n  %u" },
  m = { description = "Meeting", template = "* MEETING %? :会议:\n  %u" },
},
```

模板占位符：`%?` 光标落点、`%u` / `%U` 非主动 / 主动时间戳、
`%t` 日期、`%a` 指向速记时所在位置的链接。

### 5.3 文本对象（可视/操作符模式可用）

| 快捷键 | 选中目标 |
|--------|----------|
| `ih` / `ah` | 标题内 / 含标题行 |
| `ir` / `ar` | 子树内 / 整棵子树 |

示例：`yir` 复制当前子树内容、`dah` 删除当前标题。

### 5.4 快速定位 org 文件

- 最近用过：`<leader>fr`（fzf 最近文件）
- 全文搜内容：先 `:cd ~/Documents/org`，再 `<leader>fg` 搜关键词
- 浏览目录：`:e ~/Documents/org/` 用 netrw 打开

### 5.5 自定义 Snippet（代码片段）

blink.cmp 会自动扫描 `~/.config/nvim/snippets/` 目录，**文件名去 `.json`
后缀即 filetype**（如 `org.json` 只作用于 org 文件），与 friendly-snippets
合并使用、互不覆盖。

本配置已内置 `~/.config/nvim/snippets/org.json`，当前两个片段：

| 输入前缀 | 作用 |
|----------|------|
| `<t` + `<CR>` | 插入表格骨架（表头 + 分隔线） |
| `src` + `<CR>` | 插入代码块骨架 `#+begin_src … #+end_src`（语言名是占位符） |
| `tdo` + `<CR>` | 插入 `* TODO` 标题（含 `SCHEDULED:` 计划行） |

想加新片段，就往这个 JSON 里加一项即可，格式：

```json
{
  "片段名": {
    "prefix": "输入的前缀",
    "body": ["第一行", "第二行 ${1:占位符}", "…"]
  }
}
```

`${1:xxx}` 是光标停留的占位符，`${2:...}` 是下一个；`<Tab>` / `<S-Tab>`
在占位符间前后跳转。

---

## 六、常见问题

**Q：org 文件里按 `<leader>oo` / `<leader>ot` 为什么不是 Obsidian 的功能？**
这两个键在 org buffer 内被插件的局部映射接管（打开链接 / 改标签），
离开 org 文件即恢复 Obsidian 全局功能。`.md` 与 `.org` 各司其职。

**Q：Agenda 是空的？**
Agenda 只收集**带日期的任务**（SCHEDULED / DEADLINE / 主动时间戳）。
纯 TODO 不带日期的条目，用 `<leader>oa` → `t` 查看全部待办。

**Q：怎么彻底删除一条任务？**
它就是普通文本——光标在标题上 `dah`（删整棵子树）或直接 `dd`。

**Q：折叠状态很奇怪 / 想全部展开？**
`<S-Tab>` 循环全文折叠级别；也可用 `<leader>zR` 展开全部、
`<leader>zM` 折叠全部。

**Q：第一次打开 org 文件卡了一下？**
那是插件在编译 Org 专用 Treesitter parser，一次性，之后不再出现。

---

> 进一步阅读：Neovim 内 `:Org help`，或官方文档
> <https://nvim-orgmode.github.io>。
> 本手册只覆盖日常 20% 的功能，org buffer 内按 `g?` 探索剩下 80%。
